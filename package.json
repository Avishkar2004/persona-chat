{
  "name": "persona-chat",
  "private": true,
  "description": "Builds the React client and runs the Express server as one app for deployment",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm ci --prefix server --omit=dev && npm ci --prefix client --include=dev && CI=false npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
